# netsweep
A network discovery and security assessment tool. Point it at your network, it finds every device, scans their ports, detects services and versions, checks for known CVEs, scores each device's risk, and explains every finding in plain English, automatically.
